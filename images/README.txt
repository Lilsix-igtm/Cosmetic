Put local product images in this folder when you are ready.

Then change the image value for a product inside js/script.js, for example:
image: "images/my-cleanser.jpg"

The demo currently uses remote Unsplash image URLs so it works immediately without image files.
